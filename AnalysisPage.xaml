<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectHellsParadise.AnalysisPage"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="False" IsEnabled="False" />
    </Shell.BackButtonBehavior>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <Button Grid.Row="0" Text="←" WidthRequest="40" HeightRequest="40" HorizontalOptions="Start" Command="{Binding GoBackCommand}"></Button>
            
            <Label Grid.Row="1" Text="{Binding Ranking}" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="300" HeightRequest="30"></Label>
            
            <lvc:PolarChart
                Series="{Binding Series}"
                AngleAxes="{Binding AngleAxes}"
                TooltipTextSize="12"
                LegendPosition="Left"
                Grid.Row="2">
            </lvc:PolarChart>
            
            
        </Grid>
    </ContentPage.Content>
</ContentPage>